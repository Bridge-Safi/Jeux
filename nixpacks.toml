[phases.install]
cmds = ["npx --yes pnpm@10 install --no-frozen-lockfile"]

[phases.build]
cmds = ["npx --yes pnpm@10 --filter @workspace/3d-game run build"]

[start]
cmd = "npx serve artifacts/3d-game/dist/public --listen $PORT"

